apiVersion: v1
kind: ServiceAccount
metadata:
  name: oficina-api
  # Para usar S3 via IRSA, o overlay anota:
  #   eks.amazonaws.com/role-arn: arn:aws:iam::<acc>:role/oficina-api-s3
  # (role criada à parte). Por padrão a app usa FILESYSTEM_DISK=local.
